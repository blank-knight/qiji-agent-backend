; 生产环境配置模板：复制为 .env 后按实际填写
; 注意：type 必须写 mysql（缺省值是 sqlite，仅本地开发用）

[app]
debug = false

[database]
type = mysql
hostname = 127.0.0.1
database = qiji_admin
username = your_db_user
password = your_db_password
hostport = 3306
prefix = fa_
